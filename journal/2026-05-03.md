# 開発日誌 - 2026-05-03

## 今日やったこと

### Phase 2.5方針確認

- Phase 2.5の計画内容を確認し、方針OKとした
- Phase 3のCursor/統計/FFTへ入る前に、実地検証を効率よく回すためのUI改善を優先する
- 最初の対象は低リスクなデータロードUI改善に絞る
- wheel操作分離とbox zoomは有用だが、ロードUI整理後に回す

## 判断・設計メモ

### Phase 2.5の目的

- 実ログを何度も開き、schemaを確認し、channelを選び、load/addして波形確認する一連の操作を速くする
- 解析機能を増やす前に、実地検証の回転数を上げる
- 現状の課題は以下:
  - Parquet pathが直打ち
  - channel選択とload操作が離れている
  - loaded / loading / unloaded状態が一覧で分かりにくい

### 作図寄り機能の扱い

- 正式な `.pyc.json` 互換や作図設定仕様はPhase 4へ残す
- Phase 2.5では、Rust版内部の軽量な `session preset` として扱う
- 保存対象は、実地検証で便利な最小範囲に限定する:
  - parquet path
  - row構成
  - channel path
  - Line / Step
  - visible
  - color override
  - line width
  - Y Auto / Manual range
  - X range

## 次回の着手ポイント

### まず実装する

1. Parquet選択UI
   - Browse / file picker
   - recent path
   - path存在確認、拡張子チェック
   - schema load失敗時の表示改善

2. Schema / channel一覧UI
   - rows / time range / 推定sample rate / numeric channel数の要約
   - channel検索/filter
   - channelごとの loaded / loading / unloaded 表示
   - channel一覧から直接 Load / Add to row
   - 複数channel選択と Load checked

3. 低リスク作図補助: session preset
   - Rust版内部の軽量presetとして保存/復元
   - `.pyc.json` 互換とは分けて扱う

4. ロード済みcache操作
   - loaded channel listの整理
   - channel単位unload
   - all unload
   - loaded済みchannelを別rowへ追加しやすくする

### その後の見通し

- Phase 2.5後半:
  - wheel操作分離
  - box zoom
- Phase 3:
  - Cursor A/B
  - Δt / 周波数換算
  - 区間統計
  - FFT
- Phase 4:
  - `.pyc.json` 読み込み互換
  - 正式な作図設定保存/ロード
  - 配布・運用機能
