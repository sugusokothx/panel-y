# 開発日誌 - 2026-04-30

## 今日やったこと

### Rust Phase 1: X軸 pan/zoom と表示範囲 envelope 再抽出

- `code/rust_phase1` に X軸 pan/zoom を実装
  - ドラッグ: pan
  - ホイール/ピンチ: cursor位置基準 zoom
  - 横スクロール: pan
  - ダブルクリック / Reset X Range: 全体表示
- `PanelYApp` に `view_range` を追加し、現在のX表示範囲を状態として保持
- 表示幅に応じて envelope bucket 数を決めるようにした
  - 最小 128 buckets
  - 最大 8192 buckets
  - 通常は描画領域のpixel幅相当
- `WaveformData::min_max_envelope_for_range()` を追加
  - `time.partition_point()` で表示範囲に対応する index 範囲を取得
  - 表示範囲内だけを min/max envelope として再抽出
  - pan/zoom 中も描画点数は元sample数ではなく bucket 数に比例する
- 操作により実際に `view_range` が変わった場合だけ envelope を無効化し、不要な再抽出を避けるようにした
- `cargo fmt`, `cargo test` を実行
  - 6 tests passed

### GUI 手動確認

- `test_100k.parquet` などで X軸操作を確認
- 手動確認結果:
  - ドラッグ pan: OK
  - ホイール/ピンチ zoom: OK
  - 横スクロール pan: OK
  - ダブルクリック / Reset X Range: OK

### Phase 1 CLIベンチ追加

- `main.rs` に `--bench-channel` を追加
  - Parquetから `time + 選択ch` だけを読み込む時間
  - full range envelope 抽出時間
  - pan/zoom相当の24種類の表示範囲 envelope 再抽出時間
  - draw point数、bucket size、平均/最大時間を出力
- `README.md` に `--bench-channel` の実行例を追記
- `docs/rust_migration_phase1_benchmark_results.md` を新規作成し、結果を記録

### medium / large ベンチ結果

- `panely_medium_2m_8ch.parquet / sine_50Hz`
  - rows: 2,000,000
  - array memory: 22.9 MiB
  - read: 0.039 s
  - full envelope: 0.003 s
  - visible envelope 24 runs: total 0.012 s
  - avg: 0.0005 s
  - max: 0.0013 s
- `panely_large_10s_1mhz_9ch.parquet / iu`
  - rows: 10,000,000
  - array memory: 114.4 MiB
  - read: 0.278 s
  - full envelope: 0.013 s
  - visible envelope 24 runs: total 0.059 s
  - avg: 0.0025 s
  - max: 0.0065 s
- largeでも、表示範囲 envelope 再抽出は十分軽いことを確認

### 連続操作相当の stress 測定

- `main.rs` に `--stress-channel` を追加
  - 表示範囲 envelope 再抽出を指定回数繰り返す
  - `ps` によるRSSをブロックごとに記録
- `panely_large_10s_1mhz_9ch.parquet / iu` で 1000 runs を実行
  - wall: 3.179 s
  - measured total: 3.160 s
  - avg: 0.0032 s
  - max: 0.0090 s
  - RSS before load: 9.0 MiB
  - RSS after load: 130.0 MiB
  - RSS after full envelope: 130.0 MiB
  - RSS after stress: 130.0 MiB
- 200 / 400 / 600 / 800 / 1000 runs の各ブロックでも RSS は 130.0 MiB のまま
- 連続 pan/zoom 相当の再抽出でメモリが増え続けないことを確認

### large GUI 主観評価

- `panely_large_10s_1mhz_9ch.parquet` をGUIで読み込み確認
- `Load Selected Channel` から波形表示までの体感は約 0.5 s
  - 小さいデータより初回表示は明確に長い
  - CLI read 0.278 s に、GUI側の初回 envelope 抽出・描画更新が乗ったものとして妥当と判断
- 波形表示後の操作感は良好
  - pan: 問題なし
  - 拡大: 問題なし
  - X range reset: 問題なし
  - 波形観察の操作としては実用上ストレスなし

### Phase 1評価とPhase 2設計方針の整理

- `docs/rust_migration_phase1_evaluation.md` を新規作成
  - Phase 1の合格条件に対する結果を整理
  - Rust方針を継続し、Phase 2へ進む判断を記録
  - 90M点級は未測定のため、Phase 2早期の補足測定課題として明記
  - `egui::Painter` は継続、本格的なtile pyramidはまだ入れない方針にした
- `docs/rust_migration_phase2_design.md` を新規作成
  - 複数チャンネル / 複数行表示の状態モデルを整理
  - raw data、view state、load stateを分離する方針を記録
  - range-aware envelopeをPhase 2初期の基本方式にし、tile pyramidは必要性が出てから追加する方針にした
  - line表示を先行し、step表示はエッジ保持をPhase 2のリスク項目として扱う方針にした
- `docs/rust_migration_plan.md` を更新
  - Phase 1をPhase 2移行可として更新
  - Phase 1評価メモとPhase 2設計方針への参照を追加

## 判断・設計メモ

- P1-08「X軸 pan/zoomを行う」は完了扱い
- P1-09「表示範囲に応じたmin/max envelopeを使う」は完了扱い
- 10M行データでも、読み込み後の観察操作は十分軽い
- 表示処理は全点数ではなく、表示幅相当の bucket 数に比例する形になった
- 1ch縦切りの範囲では、Rust + egui + Parquet projection + range-aware envelope の構成は成立している
- 初回表示0.5 s程度は許容範囲
  - Phase 2では必要に応じて非同期読み込み、progress表示、読み込み中UI無効化を検討する
- RSS stress結果から、表示範囲 envelope の再抽出でメモリが増え続ける懸念は小さい
- Phase 1の主要リスク確認は完了し、Phase 2へ移行可能と判断した
  - 90M点級は未測定のため、Phase 2早期の補足測定として残す
- Phase 1評価として、Rust移植は継続する
  - 10M行データでは読み込み後のpan/zoomが十分軽い
  - Dash/Plotly版よりメモリ使用量とズーム相当処理が明確に軽い
  - ただし90M点級は未測定のため、Phase 2早期に補足測定する
- Phase 2初期では、本格的なLOD/tile pyramidは入れず、range-aware envelopeで複数行・複数chへ拡張する
  - 必要性が出た場合だけtile pyramidまたは`wgpu` custom rendererを検討する

## 詰まったこと・解決策

- RSS測定のため `ps` を呼び出したが、sandbox内では `operation not permitted` になった
  - `cargo run --release` を承認付きで実行し、外側で `ps` を使える状態にしてstress測定した
- 全体表示のままドラッグした場合、範囲が実質変わらないのに envelope を無効化しうる箇所があった
  - `next_range != current_range` の場合だけ envelope を破棄するように修正

## ここまでの状態

- 今回の最後の作業は文書整理のみで、Rustコードの追加変更はしていない
- 変更中の主なファイル:
  - `docs/rust_migration_phase1_evaluation.md`
  - `docs/rust_migration_phase2_design.md`
  - `docs/rust_migration_plan.md`
  - `journal/2026-04-30.md`
- `git diff --check` は問題なし
- 次にコードへ入る場合は、Phase 2の `P2-01 状態モデル整理` から始める

## 次回の着手ポイント

- Phase 2実装に着手する
  - data/view/load stateの分離
  - 複数channelをロードしてcacheする構造
  - 1行複数channel重ね表示
  - 複数行表示
  - X軸pan/zoomの全行同期
- Phase 2早期に補足測定する
  - 複数chロード時のメモリ
  - 複数行表示時のpan/zoom応答
  - 90M点級または実機ログ相当データ

---

## Phase 2実装着手

### P2-01 / P2-02 / P2-03一部

- `code/rust_phase1/src/main.rs`
  - `PanelYApp` 直下にあった単一 `schema` / `waveform` / `envelope` / `view_range` を分離
  - `DatasetState`
    - `parquet_path`
    - `schema`
    - dataset単位の `shared_time`
    - `loaded_channels: ChannelStore`
  - `ViewState`
    - `selected_channel`
    - `x_range`
    - `rows: Vec<PlotRow>`
  - `LoadState`
    - `status`
    - `pending_jobs`
    - `progress`
    - `error`
  - `ChannelStore`
    - `raw_by_channel`
    - `envelope_cache`
    - `envelope_context`
- `code/rust_phase1/src/parquet_waveform.rs`
  - UI用に `TimeData` と `ChannelData` を追加
  - `read_time_column()` を追加し、timeをdataset単位で保持できるようにした
  - `read_channel_values()` を追加し、channel値だけをon demandで読めるようにした
  - envelope生成を `time: &[f64]` と `values: &[f32]` の組から作れる形へ分離した
- UI
  - `Load / Add Channel` で選択channelを1行目へ追加
  - 既ロードchannelはraw cacheから再利用
  - 複数channelを1行目に重ね表示
  - `Remove` で行から外してもraw cacheは保持
  - 表示range / bucket数が同じ間はchannelごとのenvelope cacheを再利用

## 検証

- `cargo fmt`
- `cargo check`
- `cargo test`
  - 7 tests passed
- `cargo run -- --load-channel ../proto_3_1b/data/test_100k.parquet sine_50Hz`
  - CLIの既存単一channel読み込みが引き続き動くことを確認
- `cargo run --release -- --bench-multi-channel ...`
  - Phase 2の共有time + 複数channel cache構造でRSSを測るCLIを追加

## 判断・設計メモ

- P2-01は完了扱い
- P2-02は同期読み込み版として完了扱い
  - 非同期化は設計書通りP2-10で扱う
- P2-03は「1行目への複数channel重ね表示」まで完了
  - row追加・削除やstyle編集は未実装
- GUIで `test_100k.parquet` / `sine_50Hz` の表示を確認済み
  - 1行目へのchannel追加・表示経路は動作している
- 共有time + channel値cacheにしたため、複数channelロード時にtime配列をchannelごとに重複保持しない構造になった
- envelope cacheは現在の表示range / bucket数の範囲に絞っており、pan中の過去rangeを蓄積しない

### medium / large 複数chロード時メモリ測定

- medium: `panely_medium_2m_8ch.parquet`
  - rows: 2,000,000
  - channels: 8
  - time array: 15.3 MiB
  - channel arrays total: 61.0 MiB
  - raw cache memory: 76.3 MiB
  - RSS
    - before schema: 9.0 MiB
    - after time: 29.2 MiB
    - after all channels: 91.0 MiB
    - after envelopes: 91.2 MiB
  - 8ch envelope build total: 0.029 s
- large: `panely_large_10s_1mhz_9ch.parquet`
  - rows: 10,000,000
  - channels: 9
  - time array: 76.3 MiB
  - channel arrays total: 343.3 MiB
  - raw cache memory: 419.6 MiB
  - RSS
    - before schema: 9.0 MiB
    - after time: 91.0 MiB
    - after all channels: 434.8 MiB
    - after envelopes: 435.0 MiB
  - 9ch envelope build total: 0.177 s

判断:

- 複数chロード時のRSSは、共有time 1本 + channelごとのf32配列の理論値に近い
- envelope cache追加後のRSS増加はmedium/largeとも約0.2 MiBで小さい
- large 9ch全ロードでも約435 MiBで、10M行級の全channel表示準備としては許容範囲
- 90M点級は未測定のまま残る

### large GUI 複数ch主観評価

- `panely_large_10s_1mhz_9ch.parquet` をGUIで複数ch表示確認
- 2ch目の `Load / Add Channel` のほうが、1ch目のLoadより早く見える
  - 1ch目でtimeを読み、2ch目以降は共有time cacheを再利用してchannel値だけ読むため、挙動として妥当
- largeでも、表示後の操作感はこれまでのデータとの差を感じない
  - pan/zoom操作は10M行級・複数ch表示でも実用上問題なし

### X range操作とbox zoom方針

- 現状のX range操作
  - マウスホイール縦スクロール: X軸zoom
  - トラックパッドpinch: X軸zoom
  - 左右スクロール: X軸pan
  - 左ドラッグ: X軸pan
  - ダブルクリック: 全体表示reset
  - `Reset X Range` ボタン: 全体表示reset
- Plotlyのような矩形範囲ドラッグによるbox zoomは未実装
- box zoomは急がない
  - 後付けできる構造にする
  - `ViewState` に `interaction_mode` とdrag開始/現在位置を持たせれば実装可能
- 入れるタイミング
  - P2-04 複数行表示
  - P2-05 X軸pan/zoom/resetの全行同期
  - その直後の操作性改善としてbox zoomを追加するのが適切
- 理由
  - box zoomは、どのplot rect上でdragしているか、矩形overlayをどこに描くか、確定したX rangeを全行にどう反映するかに関わる
  - そのため複数行layoutとX軸同期が固まってから入れるほうが安全

## 次回の着手ポイント

- P2-03の残り確認
  - mediumで複数ch GUI操作感を見る
- P2-04 複数行表示
  - row追加・削除
  - rowごとのchannel構成
- P2-05 X軸同期
  - 複数行になった時点でpan/zoom/resetが全行に効くことを確認
- P2-05後の操作性改善
  - box zoom対応を検討する

---

## Phase 2継続: P2-04 複数行表示

### 実装

- `code/rust_phase1/src/main.rs`
  - `ViewState` に `selected_row_id` と `next_row_id` を追加
  - row追加・削除を実装
  - `Load / Add` の投入先を「選択中row」に変更
  - row内の重複channel追加は拒否し、別rowへの同一channel表示は許容する形にした
  - 中央描画領域をrow数で縦分割し、rowごとに独立したY rangeで描画するようにした
  - envelope cacheは従来通りchannel/range/bucket単位で共有し、同一channelを複数rowに出してもraw dataとenvelopeを重複保持しない
  - どのrow上で操作しても、`ViewState::x_range` 1つを更新するためX軸pan/zoom/resetは全row同期の構造になっている
- 左ペイン
  - `Add Row`
  - row選択
  - `Delete`
  - row内channelの `Remove`
  - `Load / Add to Row N` 表示

### 検証

- `cargo fmt`
- `cargo test`
  - 11 tests passed
  - row追加、選択rowへのchannel追加、row削除後の選択維持、複数rowレイアウトrectの単体テストを追加
- `cargo run -- --load-channel ../proto_3_1b/data/test_100k.parquet sine_50Hz`
  - 100,000 samples
  - read: 0.047 s
  - envelope: 4,000 buckets / 8,000 draw points
- `cargo run -- --bench-multi-channel ../proto_3_1b/data/test_100k.parquet sine_50Hz pwm_1kHz`
  - 2 channels
  - raw cache memory: 1.5 MiB
  - envelope total: 0.002 s
- `git diff --check`
  - 問題なし

### 判断・設計メモ

- P2-04はコード上は完了扱い
- P2-05は状態モデル上は全行同期になっている
  - 複数行GUIでの手動pan/zoom/reset確認は次回継続
- row削除は最後の1行は残す運用にした
  - 空rowのままチャンネルを追加できるため、UI状態が完全に空になるより扱いやすい
- rowごとのY軸は、現時点では表示中channelのauto rangeのみ
  - manual Y rangeはP2-08で扱う

## 次回の着手ポイント

- 複数行GUIの手動確認
  - Add Row
  - row選択後の `Load / Add to Row N`
  - Delete / Remove
  - pan/zoom/resetが全rowに同期すること
- P2-05確認完了後、P2-06 line / step表示へ進む

---

## Phase 2継続: 複数行GUI手動確認とスクロール対応

### 手動確認結果

- 複数行GUIの手動操作確認
  - 複数行にわたる `Delete` / `Remove`: OK
  - pan / zoom / reset の全row同期: OK
  - 操作感は軽快で問題なし
- P2-04 / P2-05は手動確認まで完了扱い

### 追加対応

- rowを増やしたときに左のDataset/Rowsエリアと波形エリア下部が見切れ、スクロールできない問題を修正
- `code/rust_phase1/src/main.rs`
  - 左ペインを `egui::ScrollArea::vertical()` でラップ
  - 波形ペインを縦スクロール化
  - rowごとの最小表示高さを `MIN_WAVEFORM_ROW_HEIGHT = 180.0` として、行数が増えた場合は圧縮しすぎずスクロールで見る形に変更
  - 波形エリア上のホイール操作は従来通りX zoomとして残すため、波形側の縦移動はスクロールバー操作を基本にした

### 検証

- `cargo fmt`
- `cargo check`
- `cargo test`
  - 11 tests passed
- `git diff --check`
  - 問題なし

## 次回の着手ポイント

- GUIでスクロール対応後の見切れ解消を確認する
- 問題なければ P2-06 line / step表示へ進む

### 追加手動確認

- row追加・削除時の波形row高さ自動調整: OK
- 選択rowへの `Load / Add`: OK
- `X range` が全row共有のまま維持されること: OK

メモ:

- 「rowごとの縦分割描画」は、中央の波形表示領域をrow数に応じて縦方向に分割し、各rowを独立したplot領域として描く、という意味
- データやX軸を分割する意味ではない
- X軸rangeは全rowで共有し、Y軸rangeだけrowごとにauto rangeで別々に計算している

### UI改善メモ

- 現時点の性能検証段階では、波形エリア上のホイール操作をX zoomとして残し、波形側の縦移動はスクロールバー操作を基本にする現仕様でOK
- 後続のUI設計改善候補:
  - 波形エリアのX zoom: `Ctrl + ホイール`
  - 波形側の縦移動: ホイールのみ
- この分離にすると、row数が増えたときの縦移動とX zoomを自然に使い分けられる可能性が高い
- 実装時期は、性能検証とPhase 2主要機能の進捗を見て判断する

---

## Phase 2継続: P2-06a line / step表示

### 実装

- `code/rust_phase1/src/main.rs`
  - `DrawMode`
    - `Line`
    - `Step`
  - `RowChannel` に `draw_mode` を追加
  - row内channelごとに `Line` / `Step` を切り替えるComboBoxを追加
  - 既存のline表示は従来通りrange-aware min/max envelopeを使う
  - step表示は、表示範囲のサンプル数が `MAX_EXACT_STEP_SAMPLES = 12,000` 以下ならraw sampleから正確な階段線を描く
  - step表示では表示範囲開始直前のsample値を引き継ぎ、範囲途中から見ても左端の状態が途切れないようにした
  - step表示の高密度範囲では、現時点では性能優先でmin/max envelopeへfallbackする

### 検証

- `cargo fmt`
- `cargo check`
- `cargo test`
  - 13 tests passed
  - 追加テスト:
    - channel追加時のdefault draw modeは `Line`
    - step traceが表示範囲開始直前の値を引き継ぐ
    - step traceがサンプル数上限を超えた場合にexact描画を避ける
- `cargo run -- --load-channel ../proto_3_1b/data/test_100k.parquet pwm_1kHz`
  - 100,000 samples
  - read: 0.030 s
  - envelope: 4,000 buckets / 8,000 draw points

### 判断・設計メモ

- P2-06は「低密度時の正確なstep描画 + 高密度fallback」まで実装
- 高密度stepのエッジ保持は未完
  - PWM/gate系の拡大率ごとの見え方をGUIで確認してから、変化点保持方式を追加する
- 次はGUIで `pwm_1kHz` / `gate_pwm` 相当を使い、Line/Step切替と拡大時のエッジ表示を確認する

### GUI確認結果

- `pwm_1kHz`, `noise` データで、拡大状態の `Step` / `Line` 切替: OK
- エッジの見え方が正しいかを検証するには、`pwm_1kHz` の1サンプル遅れ・進みデータを重ねると判断しやすい

### step edge検証データ追加

- `code/proto_3_1b/generate_step_validation_data.py` を追加
  - `time`
  - `pwm_1kHz`
  - `pwm_1kHz_delay_1sample`
  - `pwm_1kHz_advance_1sample`
- 生成先:
  - `code/proto_3_1b/data/step_validation_100k.parquet`
  - `.gitignore` の `*.parquet` 対象なので生成ファイル自体はGit管理しない
- 生成コマンド:
  - `code/.venv/bin/python code/proto_3_1b/generate_step_validation_data.py`
- Rust側確認:
  - `cargo run -- --schema ../proto_3_1b/data/step_validation_100k.parquet`
    - 100,000 rows
    - channels: `pwm_1kHz`, `pwm_1kHz_delay_1sample`, `pwm_1kHz_advance_1sample`
  - `cargo run -- --load-channel ../proto_3_1b/data/step_validation_100k.parquet pwm_1kHz_delay_1sample`
    - 100,000 samples
    - read: 0.027 s
    - envelope: 4,000 buckets / 8,000 draw points

### 次回の着手ポイント

- `step_validation_100k.parquet` をGUIで読み、3つのPWM列を同じrowにStep表示で重ねる
- 1サンプル進み/遅れが画面上で期待通り左右に見えるかを確認する
- 問題なければ高密度stepのエッジ保持方式に進む

### step edge GUI確認結果

- `step_validation_100k.parquet` で以下3列を同じrowに重ねて確認
  - `pwm_1kHz`
  - `pwm_1kHz_delay_1sample`
  - `pwm_1kHz_advance_1sample`
- `Line` / `Step` 表示の確認: OK
- 1サンプル進み/遅れの相対関係: 正確
- P2-06aの低密度step描画は、専用検証データでもOK扱い

---

## ここまでの状態整理

### 完了扱い

- P2-04 複数行表示
  - row追加・削除
  - 選択rowへの `Load / Add`
  - rowごとの個別plot領域描画
  - row追加・削除時の波形row高さ自動調整
- P2-05 X軸同期
  - `X range` は全row共有
  - pan / zoom / reset の全row同期はGUI確認済み
- P2-06a line / step表示
  - channelごとの `Line` / `Step` 切替UI
  - 低密度表示範囲での正確なraw step描画
  - 表示範囲開始直前の値を引き継ぐstep描画
  - 1サンプル進み/遅れ検証データで相対エッジ位置が正確なことを確認

### 変更中の主なファイル

- `code/rust_phase1/src/main.rs`
  - 複数row状態、row操作UI、縦スクロール、Line/Step描画
- `code/rust_phase1/README.md`
  - step edge検証データの生成・読み込み手順
- `code/proto_3_1b/generate_step_validation_data.py`
  - `pwm_1kHz` と1サンプル進み/遅れ列を持つ検証Parquet生成スクリプト
- `docs/rust_migration_phase2_design.md`
  - Phase 2進捗メモ更新
- `journal/2026-04-30.md`
  - 本日の作業ログ

### 検証済み

- `cargo fmt`
- `cargo check`
- `cargo test`
  - 13 tests passed
- `cargo run -- --load-channel ../proto_3_1b/data/test_100k.parquet pwm_1kHz`
- `cargo run -- --schema ../proto_3_1b/data/step_validation_100k.parquet`
- `cargo run -- --load-channel ../proto_3_1b/data/step_validation_100k.parquet pwm_1kHz_delay_1sample`
- `code/.venv/bin/python -m py_compile code/proto_3_1b/generate_step_validation_data.py`
- `git diff --check`

### 生成済みだがGit管理しないファイル

- `code/proto_3_1b/data/step_validation_100k.parquet`
  - `.gitignore` の `*.parquet` 対象

### 残課題・次候補

- P2-07 style設定
  - color
  - line width
  - visible
- UI改善候補
  - 将来的に `Ctrl + ホイール = X zoom`
  - `ホイールのみ = 波形側の縦スクロール`
  - 現時点の性能検証段階では未実装でOK

---

## Phase 2継続: P2-06b 高密度stepのエッジ保持

### 確認

- `gate_pwm` は1 MHz sample / 20 kHz PWMのため、中域ズームでもvisible sample数が `MAX_EXACT_STEP_SAMPLES = 12,000` を超えやすい
- 一方で、0.1s-0.3s程度の表示なら変化点数は4,000-12,000程度で、直接Step描画できる範囲だった
- sample数だけでmin/max envelopeへfallbackすると、エッジ時刻を不要に失うため、変化点保持方式は必要と判断
- 確認値:
  - `gate_pwm`
    - 0.1s: 100,001 samples / 4,000 changes -> edge-step対象
    - 0.2s: 200,001 samples / 8,000 changes -> edge-step対象
    - 0.3s: 300,001 samples / 12,000 changes -> edge-step対象
    - 0.5s: 500,001 samples / 20,000 changes -> envelope fallback
  - `pwm_10kHz`
    - 0.2s: 20,001 samples / 4,000 changes -> edge-step対象
    - 0.5s: 50,001 samples / 10,000 changes -> edge-step対象
    - 1.0s: 100,001 samples / 20,000 changes -> envelope fallback

### 実装

- `MAX_STEP_CHANGE_POINTS = 12,000` を追加
- Step表示の選択順を以下に変更
  - raw sample Step: visible sample数が `MAX_EXACT_STEP_SAMPLES` 以下
  - change-point Step: visible sample数は多いが変化点数が `MAX_STEP_CHANGE_POINTS` 以下
  - min/max envelope: 変化点数も多すぎる場合
- change-point Stepでは、表示範囲開始直前の状態を引き継ぎ、値が変わったsample時刻だけを保持する
- GUI status / plot labelにedge-step数を表示するようにした

### 検証

- `cargo fmt`
- `cargo check`
- `cargo test`
  - 16 tests passed
  - 追加テスト:
    - sample数が多い場合でも変化点だけを保持する
    - 表示範囲開始直前の状態をchange-point Stepでも引き継ぐ
    - 変化点数上限を超えた場合はchange-point Stepを避ける
- `cargo run -- --load-channel ../proto_3_1b/data/panely_large_10s_1mhz_9ch.parquet gate_pwm`
  - 10,000,000 samples
  - read: 1.681 s
  - envelope: 4,096 buckets / 8,192 draw points
- `cargo run -- --load-channel ../proto_3_1b/data/panely_medium_2m_8ch.parquet pwm_10kHz`
  - 2,000,000 samples
  - read: 0.370 s
  - envelope: 4,090 buckets / 8,180 draw points

### GUI確認結果

- `gate_pwm` を `Step` 表示で確認
- visible sample数が多く、change-point Step対象になる表示範囲: 見え方に違和感なし
- さらに広域でmin/max envelopeへfallbackする表示範囲: 見え方に違和感なし
- P2-06bはGUI確認済みとしてOK扱い

### 残課題・次候補

- UI改善候補
  - 将来的に `Ctrl + ホイール = X zoom`
  - `ホイールのみ = 波形側の縦スクロール`

---

## Phase 2継続: P2-07 style設定

### 実装

- `RowChannel` にstyle状態を追加
  - `visible`
  - `color_override`
  - `line_width`
- channel追加時のdefaultは従来表示と同じになるようにした
  - visible: ON
  - color: 既存palette
  - line width: 1.25
- Rows UIに以下を追加
  - visible checkbox
  - color picker
  - color reset
  - line width input
- `VisibleTrace` にcolor / line widthを渡し、Line / Step / min-max envelopeへ反映するようにした
- hidden channelはtrace生成前に除外し、描画・Y auto range・plot label / status集計から外れるようにした
- row内channelがすべてhiddenの場合は、波形エリアに `All channels are hidden` と表示するようにした

### 検証

- `cargo fmt`
- `cargo check`
- `cargo test`
  - 17 tests passed
  - 追加テスト:
    - channel追加時のstyle default
    - hidden channelはvisible channelとして数えない

### GUI確認結果

- visible ON/OFF: OK
- line width:
  - min widthは0.5
  - width設定は即時反映
  - Dash実装時より改善
- custom color / reset:
  - palette色から手動色へ変更できる
  - resetでpalette色に戻る
  - row内channel単位で期待通り動作
- P2-07はGUI確認済みとしてOK扱い

### 残課題・次候補

- UI改善候補
  - 将来的に `Ctrl + ホイール = X zoom`
  - `ホイールのみ = 波形側の縦スクロール`

---

## Phase 2 方針更新: 次ステップ順序

- P2-07 style設定のGUI確認完了を受け、次の実装順を `P2-09 hover同期` -> `P2-08 Y軸範囲指定` に変更する
- hover同期は複数row表示の操作感に直結するため、Y軸manual/autoより先に入れる
- P2-09の想定スコープ:
  - hover X位置の全row同期
  - 全rowへの縦ライン表示
  - visible channelの値readout
- P2-08はその後、rowごとのauto/manual Y rangeとして実装する
- Cursor A/B、Δt、区間統計、FFTなどの解析機能は従来通りPhase 3扱い

---

## Phase 2 P2-09 hover同期 実装

- `ViewState::hover_x` を追加し、plot上のpointer位置から共通X座標を更新するようにした
- hover中は全rowの同一X位置へ縦ラインを描画する
- visible channelの値readoutを追加した
  - Line表示はnearest sample値
  - Step表示はsample-and-hold値
  - 1 rowあたり最大4 channel表示、超過分は件数表示
- 追加テスト:
  - plot X座標からtimeへの変換
  - Line hoverのnearest sample lookup
  - Step hoverのheld sample lookup
- 検証:
  - `cargo fmt -- --check`: OK
  - `cargo check`: OK
  - `cargo test`: 20 tests passed

### GUI確認結果

- 確認データ:
  - `test_100k.parquet`
  - `panely_large_10s_1mhz_9ch.parquet`
- 複数row hover: OK
- Line / Stepともに値表示が更新され、値を取得できている
- 表示チェックを外したchannelはhover値readoutから除外される
- plotエリア外にカーソルがあるときはhover値表示が消える
- X軸右半分にカーソルがあるときはreadoutが左側、左半分にあるときは右側へ瞬時に切り替わる
- hover cursorと値更新は軽快
- P2-09はGUI確認済みとしてOK扱い

---

## Phase 2 P2-08 Y軸範囲指定 実装

- `PlotRow` に `RowYRange` を追加し、rowごとにY軸範囲設定を持てるようにした
- Y軸モード:
  - Auto: visible channelの値範囲から従来通りpadding付きで自動算出
  - Manual: rowごとのmin/max入力をそのまま描画範囲に使う
- Row UIにY軸Auto/Manual切替、manual min/max入力、manual resetを追加した
- Manual範囲で値がplot外に出る場合に備え、trace描画をplot rect内でclipするようにした
- 追加テスト:
  - 新規rowのY range default
  - Auto Y rangeのpadding
  - Manual Y rangeのmin/max反映
  - Manual Y rangeの逆順入力正規化
- 検証:
  - `cargo fmt -- --check`: OK
  - `cargo check`: OK
  - `cargo test`: 23 tests passed

### GUI確認結果

- Manual min/max反映: OK
- 初期値は `-1..1`
- Reset時は初期値 `-1..1` に戻る
- Manual設定中にchannelをhiddenにしても、plotのmin/maxはmanual設定を保持する
- P2-08はGUI確認済みとしてOK扱い

### 改善: Manual初期値を直近Auto範囲へ変更

- 固定初期値 `-1..1` ではなく、Manualへ切り替えた瞬間に直近のAuto表示Y範囲をmin/maxへコピーするようにした
- Manual Resetも直近のAuto表示Y範囲へ戻す
- Auto範囲がまだ未取得の場合だけ従来通り `-1..1` にfallbackする
- Auto表示Y範囲は描画データから算出し、rowごとに `last_auto` として保持する
- UI描画順の都合で最大1frame前のAuto範囲になる可能性はあるが、実操作上の問題はない前提
- 追加テスト:
  - Manual初期化がlast autoを使うこと
  - last auto未取得時にdefault `-1..1` へfallbackすること
  - Manual Reset相当の再初期化が最新last autoを使うこと
- 検証:
  - `cargo fmt -- --check`: OK
  - `cargo check`: OK
  - `cargo test`: 26 tests passed

### GUI確認結果

- Manual切り替え時の初期値: OK
- Reset時に直近Auto範囲へ戻る動き: OK
- P2-08改善はGUI確認済みとしてOK扱い

---

## Phase 2 本日まとめ: P2-09 / P2-08

### 完了扱い

- P2-09 hover同期: 実装・GUI確認完了
- P2-08 Y軸範囲指定: 実装・GUI確認完了
- P2-08改善: Manual初期値/Resetを直近Auto範囲へ変更し、GUI確認完了

### P2-09 hover同期

- `ViewState::hover_x` でhover X位置を共有し、全rowに同一X位置の縦ラインを表示するようにした
- visible channelのhover値readoutを追加した
  - Line: nearest sample値
  - Step: sample-and-hold値
  - hidden channelはreadout対象外
- readoutはカーソル位置に応じて左右へ逃がす
  - X軸右半分では左側
  - X軸左半分では右側
- plot外ではhover line/readoutを消す
- `test_100k.parquet` と `panely_large_10s_1mhz_9ch.parquet` で更新が軽快なことを確認した

### P2-08 Y軸範囲指定

- `PlotRow` に `RowYRange` を追加し、rowごとにY軸設定を持つ構造にした
- Auto:
  - visible channelの値範囲からpadding付きで従来通り算出
  - hidden channelはAuto範囲計算から除外
- Manual:
  - rowごとのmin/maxを描画範囲に使う
  - channelをhiddenにしてもManual min/maxは保持
  - traceがManual範囲外へ出る場合はplot rect内でclipする
- Manual切替時とReset時は、固定 `-1..1` ではなく直近Auto表示範囲をmin/maxへコピーする
  - Auto範囲未取得時のみ `-1..1` にfallback
  - UI描画順の都合で最大1frame前のAuto範囲になる可能性はあるが、実操作上問題なし

### 検証

- P2-09実装時:
  - `cargo fmt -- --check`: OK
  - `cargo check`: OK
  - `cargo test`: 20 tests passed
- P2-08実装時:
  - `cargo fmt -- --check`: OK
  - `cargo check`: OK
  - `cargo test`: 23 tests passed
- P2-08改善後:
  - `cargo fmt -- --check`: OK
  - `cargo check`: OK
  - `cargo test`: 26 tests passed

### 現在のPhase 2状況

- 完了済み:
  - P2-01 状態モデル整理
  - P2-02 複数channelロード/cache
  - P2-03 1行複数channel表示
  - P2-04 複数行表示
  - P2-05 X軸同期
  - P2-06 line / step表示
  - P2-07 style設定
  - P2-08 Y軸範囲指定
  - P2-09 hover同期
- 次候補:
  - P2-10 非同期読み込み
  - P2-11 theme切替
  - P2-12 性能再測定

### メモ

- P2-08 / P2-09により、複数row表示での日常的な波形確認に必要な基本操作はかなり揃った
- 次の大きなリスクは、複数channel/大規模データ読み込み時にUI threadが固まること
- P2-10ではdata/view/load stateを崩さず、job完了時だけUI stateへ反映する方針を維持する

---

## Phase 2 P2-10: 非同期読み込み

### 実装内容

- `load_selected_channel()` をUI thread同期読み込みからworker thread起動に変更した
- `mpsc` の完了通知を `PanelYApp` が各frame先頭で回収し、完了時だけ `DatasetState` / `ViewState` に反映する形にした
- 読み込み開始時に対象rowへchannelプレースホルダを追加し、plot側で `Loading waveform data...` を表示するようにした
- 読み込み中の危険操作を制限した
  - Parquet path編集とschema reloadは無効化
  - 読み込み中channelのRemoveは無効化
  - 読み込み中channelを含むrowのDeleteは無効化
- 既存cache済みchannelはworkerを起動せず即時rowへ追加する
- 同一channelが読み込み中の場合は重複jobを作らず、別rowへの表示予約だけ許可する

### 検証

- `cargo fmt --check`: OK
- `cargo check`: OK
- `cargo test`: 26 tests passed

### GUI確認: panely_large_10s_1mhz_9ch

- channel load時のjob spinner表示は体感0.3〜0.5秒程度
- 大規模データでのhover値更新とX軸スケール操作感:
  - 表示範囲が約10,000,000 samplesから2,000,000〜3,000,000 samples程度までは、高速に拡大すると少しカクつく
  - 約3,000,000 samples付近から急になめらかになる
- ダブルクリックによるXスケールリセットは瞬時に反応し、描画も即時
- P2-10の非同期読み込み自体はGUI操作感として成立。高速ズーム時の軽いカクつきはP2-12の性能再測定・描画最適化候補として扱う

### 現在のPhase 2状況

- 完了済み:
  - P2-01 状態モデル整理
  - P2-02 複数channelロード/cache
  - P2-03 1行複数channel表示
  - P2-04 複数行表示
  - P2-05 X軸同期
  - P2-06 line / step表示
  - P2-07 style設定
  - P2-08 Y軸範囲指定
  - P2-09 hover同期
  - P2-10 非同期読み込み
- 次候補:
  - P2-12 性能再測定
  - P2-option UI polish / 操作環境改善

---

## Phase 2 締め: P2-12を完了ゲートにする

### 判断

- P2-11 theme切替は性能に直接影響しないため、Phase 2必須タスクから外す
- theme切替、box zoom、wheel操作分離、細かな表示調整は `P2-option UI polish / 操作環境改善` として扱う
- Phase 2の完了ゲートは `P2-12 定量ベンチ` とする

### 理由

- P2-10までで、性能に関わる主要経路は揃った
  - 複数channel load/cache
  - 複数row描画
  - line / step
  - style visible / line width / color
  - auto/manual Y range
  - hover同期
  - 非同期読み込み
- theme切替は `egui` visualsと色パレットの切替が中心で、Parquet読み込み、raw cache、envelope生成、step抽出、hover値探索、描画点数には基本的に影響しない
- 定量ベンチ前に入れると、作業範囲は増えるが測定前提の精度向上にはつながりにくい

### 次回方針

- 次回は `P2-12 定量ベンチ` から着手する
- 測定対象:
  - 中・大データ
  - 可能なら90M点級または実機ログ相当
  - load時間、RSS、multi-channel cache、envelope生成、pan/zoom相当、hover相当
- GUI確認で見えた高速ズーム時の軽いカクつきは、P2-12で再現条件と数値を記録し、tile/LODや描画生成最適化が必要か判断する

### 現在のPhase 2状況

- 完了済み:
  - P2-01 状態モデル整理
  - P2-02 複数channelロード/cache
  - P2-03 1行複数channel表示
  - P2-04 複数行表示
  - P2-05 X軸同期
  - P2-06 line / step表示
  - P2-07 style設定
  - P2-08 Y軸範囲指定
  - P2-09 hover同期
  - P2-10 非同期読み込み
- Phase 2完了ゲート:
  - P2-12 定量ベンチ
- 任意改善:
  - P2-option UI polish / 操作環境改善

---

## Phase 2 P2-12: 定量ベンチ

### 実装

- `code/rust_phase1` に `--bench-phase2` CLIを追加した
- 測定内容:
  - time列読み込み
  - 複数channel raw cache読み込み
  - 3chごとの複数rowレイアウト生成
  - Line channelのrange-aware envelope生成
  - Step channelのraw step / change-point step / envelope fallback判定
  - hover値探索
  - RSS
- `code/rust_phase1/README.md` に実行例を追加した
- 結果を `docs/rust_migration_phase2_benchmark_results.md` に記録した

### 測定コマンド

```bash
cd code/rust_phase1
cargo run --release -- --bench-phase2 ../proto_3_1b/data/panely_medium_2m_8ch.parquet
cargo run --release -- --bench-phase2 ../proto_3_1b/data/panely_large_10s_1mhz_9ch.parquet
cargo run --release -- --bench-phase2 ../proto_3_1b/data/panely_large_10s_1mhz_9ch.parquet iu iv iw gate_pwm
```

### 結果サマリ

| case | layout | raw cache | RSS after bench | visible avg | visible max | hover |
|---|---:|---:|---:|---:|---:|---:|
| medium 全8ch | 3 rows / 8 ch | 76.3 MiB | 91.6 MiB | 8.0 ms | 22.1 ms | 0.264 us/lookup |
| large 全9ch | 3 rows / 9 ch | 419.6 MiB | 435.4 MiB | 31.3 ms | 92.5 ms | 0.439 us/lookup |
| large 代表4ch | 2 rows / 4 ch | 228.9 MiB | 244.4 MiB | 14.4 ms | 38.2 ms | 0.504 us/lookup |

### 判断

- `panely_large_10s_1mhz_9ch.parquet` は10M rows x 9chで総channel点数90M点の測定になった
- 大データ全9ch raw cacheでもRSSは約435 MiBで、メモリ面は許容範囲
- hover値探索は十分軽く、現時点の性能リスクではない
- 大データ全9chの広い表示範囲では、visible trace生成が最大92.5 ms
  - GUI確認で見えた高速ズーム時の軽いカクつきと整合する
  - 2M samples相当では約32-33 ms、500k samples相当では約9 ms、100k samples相当では約1.5-1.9 ms
- Phase 2完了ゲートとしてのP2-12は測定完了扱い
- 次の性能改善候補:
  - pan/zoom中のrange cacheまたはtile / LOD pyramid
  - Step広範囲fallback判定の早期化
  - `egui::Painter` shape生成時間のGUI側計測

### 更新ファイル

- `code/rust_phase1/src/main.rs`
- `code/rust_phase1/README.md`
- `docs/rust_migration_phase2_benchmark_results.md`
- `docs/rust_migration_phase2_design.md`
- `docs/rust_migration_plan.md`
- `journal/2026-04-30.md`
